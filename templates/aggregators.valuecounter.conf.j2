# Count the occurrence of values in fields.
[[aggregators.valuecounter]]
  ## General Aggregator Arguments:
  ## The period on which to flush & clear the aggregator.
  period = "{{ telegraf_aggregators_valuecounter_period|d() }}"
  ## If true, the original metric will be dropped by the
  ## aggregator and will not get sent to the output plugins.
  drop_original = {{ telegraf_aggregators_valuecounter_drop_original|d()|bool|lower }}
  ## The fields for which the values will be counted
  fields = [ {% for i in telegraf_aggregators_valuecounter_fields|d([]) %}"{{ i }}"{{ ', ' if not loop.last }}{% endfor %} ]
