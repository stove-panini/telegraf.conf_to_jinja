# Read metrics from local Lustre service on OST, MDS
[[inputs.lustre2]]
  ## An array of /proc globs to search for Lustre stats
  ## If not specified, the default will work on Lustre 2.5.x
  ##
  {{ '#' if telegraf_inputs_lustre2_ost_procfiles is not defined }}ost_procfiles = [ {% for i in telegraf_inputs_lustre2_ost_procfiles|d([]) %}"{{ i }}"{{ ', ' if not loop.last }}{% endfor %} ]
  #   "/proc/fs/lustre/obdfilter/*/stats",
  #   "/proc/fs/lustre/osd-ldiskfs/*/stats",
  #   "/proc/fs/lustre/obdfilter/*/job_stats",
  # ]
  {{ '#' if telegraf_inputs_lustre2_mds_procfiles is not defined }}mds_procfiles = [ {% for i in telegraf_inputs_lustre2_mds_procfiles|d([]) %}"{{ i }}"{{ ', ' if not loop.last }}{% endfor %} ]
  #   "/proc/fs/lustre/mdt/*/md_stats",
  #   "/proc/fs/lustre/mdt/*/job_stats",
  # ]
