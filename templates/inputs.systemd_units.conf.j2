# Gather systemd units state
[[inputs.systemd_units]]
  ## Set timeout for systemctl execution
  {{ '#' if telegraf_inputs_systemd_units_timeout is not defined else '' }}timeout = "{{ telegraf_inputs_systemd_units_timeout|d() }}"
  #
  ## Filter for a specific unit type, default is "service", other possible
  ## values are "socket", "target", "device", "mount", "automount", "swap",
  ## "timer", "path", "slice" and "scope ":
  {{ '#' if telegraf_inputs_systemd_units_unittype is not defined else '' }}unittype = "{{ telegraf_inputs_systemd_units_unittype|d() }}"
