# Parse a value in a specified field/tag(s) and add the result in a new metric
[[processors.parser]]
  ## The name of the fields whose value will be parsed.
  parse_fields = [ {% for i in telegraf_processors_parser_parse_fields|d([]) %}"{{ i }}"{{ ', ' if not loop.last else '' }}{% endfor %} ]

  ## If true, incoming metrics are not emitted.
  drop_original = {{ telegraf_processors_parser_drop_original|d()|bool|lower }}

  ## If set to override, emitted metrics will be merged by overriding the
  ## original metric using the newly parsed metrics.
  merge = "{{ telegraf_processors_parser_merge|d() }}"

  ## The dataformat to be read from files
  ## Each data format has its own unique set of configuration options, read
  ## more about them here:
  ## https://github.com/influxdata/telegraf/blob/master/docs/DATA_FORMATS_INPUT.md
  data_format = "{{ telegraf_processors_parser_data_format|d() }}"
